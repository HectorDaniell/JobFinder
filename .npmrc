# pnpm configuration
# Prevent creating node_modules in workspace projects
# Keep everything in root node_modules only
shamefully-hoist=true
hoist-pattern=*

# Prefer offline mode for faster installs
prefer-offline=true
