/**
 * Superficie pública de @jobfinder/documents.
 *
 * El adapter es la única pieza que consume el mundo exterior (la API); el resto
 * (mappers, exporters, modelos) son detalles internos del package.
 */
export { DocumentAdapter } from './adapter';
