{
  "name": "@jobfinder/documents",
  "version": "0.1.0",
  "description": "CV and cover letter generation (PDF, DOCX)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@jobfinder/core": "workspace:*",
    "@jobfinder/shared": "workspace:*",
    "docx": "^8.5.0",
    "pdfkit": "^0.19.1",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/pdfkit": "^0.17.6",
    "@vitest/coverage-v8": "^1.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}
