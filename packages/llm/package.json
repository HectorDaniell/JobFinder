{
  "name": "@jobfinder/llm",
  "version": "0.1.0",
  "description": "LLM adapters (Claude, embeddings)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && node scripts/copy-prompts.js",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@jobfinder/core": "workspace:*",
    "@jobfinder/shared": "workspace:*",
    "@anthropic-ai/sdk": "^0.20.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vitest": "^1.0.0",
    "@vitest/coverage-v8": "^1.0.0"
  }
}
